import { Home, Star, Flag, Calendar, Car, MapPin } from 'lucide-react';
import type { LucideIcon } from 'lucide-react';

export const iconMap: Record<string, LucideIcon> = {
  home: Home,
  star: Star,
  flag: Flag,
  calendar: Calendar,
  car: Car,
  mapPin: MapPin,
};
